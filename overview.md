# 学生成长管理平台 — 交付说明

## 成果
单文件网页应用：`student-hub.html`（57 KB，零依赖，双击即用，无需联网、无需服务器）。

## 模块结构（4 个核心模块 + 1 个总览）

| 模块 | 能力 |
|---|---|
| 总览 | 在读人数、本月互动、成果总数、平均路径进度；最近互动、最新成果、各学生路径进度条 |
| 学生管理 | 卡片列表；按姓名/班级/标签搜索，按分组、状态筛选；新增、编辑、删除（删除时提示关联数据量并级联清理）；详情档案弹窗 |
| 互动记录 | 时间线展示，按学生 + 类型（作业反馈/一对一答疑/鼓励沟通/作品点评/家长沟通）筛选；每条含主题、内容、下一步跟进；增删改 |
| 学习路径 | 左侧学生列表（带进度），右侧阶段清单；阶段支持待开始/进行中/已完成三态切换、增删改、截止日期；自动计算完成百分比 |
| 成果博客 | 每位学生一个独立博客页：头部档案卡（成果数/互动数/路径进度/入学时间）+ 成果卡片墙（作品、获奖、证书、项目、随笔）；支持发布、编辑、删除、阅读全文 |

## 关键设计决策
1. **单文件交付**：HTML/CSS/JS 全内联，无外部依赖，可直接发给同事或存 U 盘。
2. **数据存本地 localStorage**：打开即自动保存，刷新不丢。左下角提供「导出数据 / 导入」，导出为标准 JSON，可备份、迁移、多机同步。
3. **成果分享做了三种出口**：复制分享摘要（纯文本，可直接粘贴到微信群/邮件）、导出独立博客 HTML（单文件，自带打印按钮，可存 PDF）、浏览器打印。
4. **模块做了合并**：你提到的「成果展示 / 学生博客页 / 成果分享」本质是同一份数据的三种呈现，合并为一个「成果博客」模块，避免同一条成果要在三个地方重复录入。
5. **级联删除**：删除学生时同步清理其互动记录、成果、学习路径，并在确认框里明确告知条数，防止误删后产生孤儿数据。
6. **预置 4 名示例学生**（林知远、陈牧、苏晚、何一诺）及其互动、路径、成果，打开即可看到完整形态；不需要时可直接删除。

## 使用方法
1. 双击 `student-hub.html` 用浏览器打开（推荐 Chrome / Edge）。
2. 先到「学生管理」把示例学生删掉或改成自己的学生。
3. 日常：上课后记一条互动 → 阶段性更新学习路径 → 学生出成果时在「成果博客」发布。
4. 需要分享给家长：选中该学生 → 导出博客页，得到的 HTML 可直接发送或打印。

## 后续可迭代（本次未做）
- 多老师协作 / 账号权限
- 附件与图片上传（作品截图、证书扫描件）
- 日历视图看互动与阶段截止
- 数据云同步（需接入在线表格服务）

## 已知限制
- 数据保存在当前浏览器，换电脑或清理浏览器数据会丢失，务必定期用「导出数据」备份。
- 单文件纯前端，不含登录鉴权，不适合直接放到公网无保护地访问。
